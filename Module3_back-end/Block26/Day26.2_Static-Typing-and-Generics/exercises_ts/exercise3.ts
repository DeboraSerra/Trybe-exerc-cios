export interface Pizza {
  flavor: string,
  slices: 4 | 6 | 8,
}

const calabresa: Pizza = {
  flavor: "Calabres",
  slices: 8,
}

const Marguerita: Pizza = {
  flavor: 'Marguerita',
  slices: 6,
}

const Nutella: Pizza = {
  flavor: 'Nutella',
  slices: 4,
}
