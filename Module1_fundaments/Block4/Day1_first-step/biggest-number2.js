const alfa = 25;
const beta = 150;
const teta = 20;

if (alfa > beta && alfa > teta) {
  console.log(alfa);
} else if (beta > alfa && beta > teta) {
  console.log(beta);
} else if (teta > alfa && teta > beta) {
  console.log(teta);
}