const stringToUpper = (str) => str.toUpperCase();

const firstLetterStr = (str) => str[0];

const concatStrings = (str1, str2) => str1 + str2;

module.exports = {
  stringToUpper,
  firstLetterStr,
  concatStrings,
}